// Re-export keeps the file structure required by the build prompt.
// The actual types live alongside [ShopBundle] to avoid a circular import.
export 'shop_bundle.dart' show BundlePrerequisite, PrerequisiteType, PrerequisiteTypeJson;
